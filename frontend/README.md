# Inventory Control Frontend (React Scaffold)

Basic React + Vite scaffold created for Phase 1.

## Run locally

```bash
cd frontend
npm install
npm run dev
```

The dev server will start with the default Vite port and can call backend APIs at `http://localhost:3001`.
