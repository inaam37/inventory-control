{
  "name": "pantrypilot-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Backend API for PantryPilot inventory control",
  "main": "src/index.js",
  "scripts": {
    "dev": "node src/index.js",
    "test": "node --test",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:migrate:deploy": "prisma migrate deploy",
    "prisma:studio": "prisma studio"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "prisma": "^5.20.0"
  }
}
